Python-buildout
===============

This is a collection of zc.buildout recipes to automate building Python
with its dependencies from sources.

The goal is to cover most versions possible and to have self-contained python
distribution.


How to use
----------

You will need to install some build toolchain, python and zc.buildout.
Running buildout in repo should perform downloading and building.
